#!/bin/bash
. /etc/glconfig/glfs-config-global
[ $DEBUG ] && set -x  

while nsenter --mount=/media/host/proc/1/ns/mnt mountpoint ${VOLPATH}; do
  sleep 10
done
echo "mount is stale"
rm -f /glready
stop-client
